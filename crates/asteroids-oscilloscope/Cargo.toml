[package]
name = "asteroids-oscilloscope"
version = "0.1.0"
edition = "2024"
description = "SCAFFOLD: Asteroids traced on an oscilloscope in XY mode -- not yet implemented, see src/lib.rs"

[dependencies]
asteroids-core = { path = "../asteroids-core" }
